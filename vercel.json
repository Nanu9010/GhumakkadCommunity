{
  "version": 2,
  "installCommand": "pip install -r skytravel/requirements.txt",
  "buildCommand": "cd skytravel && python manage.py collectstatic --noinput",
  "outputDirectory": "skytravel/staticfiles",
  "framework": null,
  "builds": [
    {
      "src": "skytravel/api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/staticfiles/$1"
    },
    {
      "src": "/(.*)",
      "dest": "skytravel/api/index.py"
    }
  ]
}
