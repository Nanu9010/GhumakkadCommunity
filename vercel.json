{
  "version": 2,
  "installCommand": "pip install -r skytravel/requirements.txt",
  "buildCommand": "cd skytravel && python manage.py collectstatic --noinput && python manage.py migrate --noinput",
  "outputDirectory": "skytravel/staticfiles",
  "framework": null,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/skytravel/staticfiles/$1"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
